<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:background="@drawable/selector_view_bg"
    android:id="@+id/fai_item"
    >
    <ImageButton
        android:id="@+id/loc_favorite_icon"
        android:layout_width="72dp"
        android:layout_height="60dp"
        android:background="#00000000"
        android:layout_alignParentLeft="true"
        android:alpha="0.54"
        android:src="@drawable/star_black"/>
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingRight="30dp"
        android:layout_alignParentTop="true"
        android:layout_alignParentRight="true"
        android:layout_toRightOf="@id/loc_favorite_icon"
        android:layout_alignBottom="@id/loc_favorite_icon"
        android:gravity="center_vertical"
        >
        <TextView
            android:id="@+id/loc_item_name_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/new_text_color_first"
            android:textSize="@dimen/regular"
            android:singleLine="true"
            android:text="广州市火车站"
            />
        <TextView
            android:id="@+id/loc_item_address_text"
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:textColor="@color/new_text_color_second"
            android:textSize="@dimen/md_small"
            android:singleLine="true"
            android:text="广州市越秀区"
            />
    </LinearLayout>
</RelativeLayout>